# Contributing

We love contributions! See pipeline:

1. Fork `kv-creates/kv-14`
2. `pip install -e ".[dev]"`
3. `pytest`
4. Open PR - KV-14 swarm will auto-review it
## Minimalist quickstart
`pip install -r requirements.txt && pytest -q`
